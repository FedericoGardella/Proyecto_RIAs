import { FormatInsumosPipe } from './format-insumos.pipe';

describe('FormatInsumosPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatInsumosPipe();
    expect(pipe).toBeTruthy();
  });
});
