import { ProductoEnCarrito } from './producto-en-carrito';

describe('ProductoEnCarrito', () => {
  it('should create an instance', () => {
    expect(new ProductoEnCarrito()).toBeTruthy();
  });
});
