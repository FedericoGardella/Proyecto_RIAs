import { FormatProductosPipe } from './format-productos.pipe';

describe('FormatProductosPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatProductosPipe();
    expect(pipe).toBeTruthy();
  });
});
